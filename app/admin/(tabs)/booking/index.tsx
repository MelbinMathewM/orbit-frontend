import { ScrollView } from "react-native";
import BookingMain from "@/components/admin/booking/booking-main";

export default function BookingScreen() {

    return (
        <ScrollView>
            <BookingMain />
        </ScrollView>
    );
}
