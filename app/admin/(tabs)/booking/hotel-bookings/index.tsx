import HotelBookings from "@/components/admin/hotel-bookings";
import { ScrollView } from "react-native";

export default function HotelBookingScreen() {
    return (
        <ScrollView>
            <HotelBookings />
        </ScrollView>
    )
}