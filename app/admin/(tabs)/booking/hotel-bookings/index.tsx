import HotelBookings from "@/components/admin/booking/hotel-bookings";
import { ScrollView } from "react-native";

export default function HotelBookingScreen() {
    return (
        <ScrollView>
            <HotelBookings />
        </ScrollView>
    )
}