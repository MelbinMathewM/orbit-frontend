import TourPackages from "@/components/user/booking/special-package";
import { ScrollView } from "react-native";

export default function SpecialPackage() {
    return (
        <ScrollView>
            <TourPackages />
        </ScrollView>
    )
}